{
  "schema": "sli.scar.telemetry.v0.1.0",
  "description": "SCAR generation telemetry for routing quality and glue interactions.",
  "required": [
    "schema",
    "bias_block_count",
    "anchor_count",
    "token_node_map_coverage_rate",
    "scope_barrier_count",
    "avg_bias_magnitude",
    "warnings"
  ]
}
